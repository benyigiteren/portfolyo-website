# assets/

Logonu buraya `logo.webp` olarak koy. Site otomatik olarak `/assets/logo.webp`
adresinden okur (src/lib/config.ts içindeki `logo` alanı).

Logo yoksa site "YE" monogramını gösterir, bu yüzden görsel eklemeden de
site bozuk görünmez.
